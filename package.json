{
  "name": "osrs-progress",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --open",
    "dev": "cross-env VITE_API_URL=http://127.0.0.1:8787/ vite --open",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "cross-env": "^7.0.3",
    "typescript": "~5.6.2",
    "vite": "^5.4.9"
  }
}
